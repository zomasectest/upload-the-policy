<!DOCTYPE html>
<html>
<head>
  <title>Leaderboard - Upload The Policy</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap" rel="stylesheet">
  <style nonce="<%= nonce %>">
    body {
      background: linear-gradient(135deg, #1a001a, #2c003e 80%, #1a0000 100%);
      color: #fff;
      font-family: 'Share Tech Mono', monospace;
      min-height: 100vh;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    .ctf-container {
      background: rgba(30, 0, 40, 0.97);
      border-radius: 1.5rem;
      box-shadow: 0 8px 32px 0 rgba(255,0,204,0.25);
      padding: 48px 36px 32px 36px;
      max-width: 480px;
      width: 100%;
      text-align: center;
      border: 2px solid #a020f0;
      margin-bottom: 24px;
    }
    .ctf-badge {
      display: inline-block;
      background: #a020f0;
      color: #fff;
      font-size: 1em;
      padding: 6px 18px;
      border-radius: 12px;
      margin-bottom: 1.2em;
      letter-spacing: 1px;
      box-shadow: 0 0 12px #a020f088;
      font-weight: bold;
    }
    h2 {
      font-size: 2.2em;
      margin-bottom: 0.2em;
      letter-spacing: 2px;
      color: #a020f0;
      text-shadow: 0 0 12px #a020f0, 0 0 2px #fff;
    }
    table {
      width: 100%;
      margin-top: 2em;
      color: #fff;
      background: rgba(30,0,40,0.7);
      border-radius: 10px;
      overflow: hidden;
    }
    th, td {
      padding: 12px 8px;
      border-bottom: 1px solid #a020f0;
    }
    th {
      background: #a020f0;
      color: #fff;
      font-size: 1.1em;
    }
    tr:last-child td {
      border-bottom: none;
    }
    .userid {
      background: #1a001a;
      color: #ff003c;
      padding: 8px 18px;
      border-radius: 10px;
      display: inline-block;
      margin-top: 0.7em;
      font-size: 1.1em;
      letter-spacing: 1px;
      box-shadow: 0 0 8px #ff003c44;
      border: 1px solid #ff003c;
    }
    .btn-purple {
      border-color: #a020f0;
      color: #a020f0;
    }
    .btn-purple:hover {
      color: #ff003c;
      text-shadow: 0 0 8px #ff003c;
    }
    .author-link {
      font-size: 0.95em;
    }
    .username-link {
      color: #a020f0;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="ctf-container shadow-lg">
    <div class="ctf-badge">Leaderboard</div>
    <h2>Top Players</h2>
    <div class="userid">Your user ID: <%= userid %></div>
    <table class="table table-dark table-striped mt-4">
      <thead>
        <tr>
          <th>Rank</th>
          <th>Username</th>
        </tr>
      </thead>
      <tbody>
        <% function ordinal(n) {
             if (n === 1) return '1st';
             if (n === 2) return '2nd';
             if (n === 3) return '3rd';
             return n;
           }
        %>
        <% leaderboard.forEach(function(entry, idx) { %>
          <tr>
            <td><%= ordinal(idx + 1) %></td>
            <td>
              <a href="https://x.com/<%= encodeURIComponent(entry.username) %>" target="_blank" rel="noopener noreferrer" class="username-link">
                <%= entry.username %>
              </a>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
    <a class="btn btn-outline-light w-75 btn-purple" href="/">Back to Home</a>
  </div>
</body>
</html>