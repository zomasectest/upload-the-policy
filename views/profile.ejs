<!DOCTYPE html>
<html>
<head>
  <title>Profile - Upload The Policy</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #1a001a, #2c003e 80%, #1a0000 100%);
      color: #fff;
      font-family: 'Share Tech Mono', monospace;
      min-height: 100vh;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    .ctf-container {
      background: rgba(30, 0, 40, 0.97);
      border-radius: 1.5rem;
      box-shadow: 0 8px 32px 0 rgba(255,0,204,0.25);
      padding: 48px 36px 32px 36px;
      max-width: 520px;
      width: 100%;
      text-align: center;
      border: 2px solid #a020f0;
      margin-bottom: 24px;
    }
    .ctf-badge {
      display: inline-block;
      background: #a020f0;
      color: #fff;
      font-size: 1em;
      padding: 6px 18px;
      border-radius: 12px;
      margin-bottom: 1.2em;
      letter-spacing: 1px;
      box-shadow: 0 0 12px #a020f088;
      font-weight: bold;
    }
    h1 {
      font-size: 2em;
      margin-bottom: 0.5em;
      letter-spacing: 2px;
      color: #a020f0;
      text-shadow: 0 0 12px #a020f0, 0 0 2px #fff;
    }
    h2 {
      color: #ff003c;
      margin-top: 1.5em;
    }
    .author {
      margin-top: 2em;
      font-size: 1.1em;
      color: #ff003c;
      letter-spacing: 1px;
    }
    .author a {
      color: #a020f0;
      text-decoration: none;
      font-weight: bold;
    }
    form {
      margin-top: 1.5em;
    }
    label {
      display: block;
      margin: 1em 0 0.5em 0;
      font-size: 1.1em;
      color: #fff;
    }
    input[type="text"] {
      padding: 10px 14px;
      border-radius: 8px;
      border: 1px solid #a020f0;
      background: #1a001a;
      color: #a020f0;
      font-size: 1em;
      margin-top: 0.2em;
      width: 90%;
      box-shadow: 0 0 8px #a020f044;
    }
    button[type="submit"] {
      margin-top: 1.2em;
      padding: 10px 28px;
      border-radius: 8px;
      border: none;
      background: #a020f0;
      color: #fff;
      font-size: 1.1em;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 0 8px #a020f044;
      transition: background 0.2s, color 0.2s;
    }
    button[type="submit"]:hover {
      background: #ff003c;
      color: #fff;
      box-shadow: 0 0 12px #ff003c88;
    }
    #bio {
      background: #1a001a;
      color: #fff;
      padding: 14px 20px;
      border-radius: 10px;
      margin: 0.5em auto 1em auto;
      width: 90%;
      min-height: 32px;
      box-shadow: 0 0 8px #a020f044;
      word-break: break-word;
      border: 1px solid #a020f0;
    }
    a {
      color: #a020f0;
      text-decoration: none;
      font-size: 1.1em;
      transition: color 0.2s;
    }
    a:hover {
      color: #ff003c;
      text-shadow: 0 0 8px #ff003c;
    }
  </style>
</head>
<body>
  <div class="ctf-container shadow-lg">
    <div class="ctf-badge">Upload The Policy Challenge</div>
    <h1>Profile Page</h1>
    <form method="GET" action="/profile">
      <label>Bio:<br><input type="text" name="bio" value="<%= bio %>"></label>
      <button type="submit">Update</button>
    </form>
  <br>
    <% if (bio && bio.trim().length > 0) { %>
      <div id="bio">Your Input is : <%- bio %></div>
    <% } %>
    <br>
    <p><a class="btn btn-outline-light w-75 mt-3" style="border-color:#a020f0;color:#a020f0;" href="/">Back to Home</a></p>
    <div class="author">
      Challenge by <b>@zomasec</b><br>
      <span style="font-size:0.95em;">Follow on <a href="https://x.com/zomasec" target="_blank">X (Twitter)</a></span>
    </div>
  </div>
  <script nonce="<%= nonce %>">
    let url =  configURL ||  {u : "https://cdn.local/urls.js"};
    let s = document.createElement('script');
    s.src = url.u;
    s.setAttribute('nonce', '<%= nonce %>');
    document.body.appendChild(s);
  </script>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 